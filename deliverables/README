This directory that contains :
1. merged_table_E.csv : CSV file storing Table E
2. predictions_all.csv : the set of matches between Tables A and B (these matches should be stored in a single file)
3. <Deepanshu FILL THIS> : the Python script that you used to merge the two tables A and B